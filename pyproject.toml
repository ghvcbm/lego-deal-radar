[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lego-deal-radar"
version = "0.1.0"
description = "Telegram bot foundation for LEGO Architecture deal monitoring."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
lego-deal-radar = "python_project.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]